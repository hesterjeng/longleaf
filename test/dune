(executable
 (name test)
 (modes exe)
 (libraries alcotest astring longleaf-lib eio eio.unix piaf uri))

; This fails at runtime, so just build the executable

(rule
 (alias runtest)
 ; (context default)
 (package longleaf-lib)
 (action
  (run %{dep:test.exe})))
